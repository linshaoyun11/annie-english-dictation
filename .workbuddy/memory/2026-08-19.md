# 2026-08-19

## 新用户默认配置调整

- `src/lib/users.ts`：`DEFAULT_CONFIG` 由 `{ curriculum: "renjiao", accent: "us" }` 改为 `{ curriculum: "waiyanshe", accent: "us" }`
- 影响范围：仅新注册用户（`registerUser` 用 `{...DEFAULT_CONFIG}` 初始化）；老用户 config 字段已持久化不受影响；`loadUsers` 的旧数据兼容补默认也随新值（老用户无 config 时补外研社，属预期迁移行为）
- 注册链路自动生效：注册 → `handleLogin` → `loadProgress(user.id, "waiyanshe")` 加载外研社进度
- 验证：`npm run build` 通过，dev server 200

## 学习界面 UI 精致化（正规 App 观感 + 反馈升级）

- `LearningCard.tsx` 重写反馈展示；`index.css` 新增 4 个动画：badgePop（徽章弹入）、checkDraw（对勾描边 stroke-dashoffset 26→0）、ringOut（扩散圆环）、autoNextBar（1.8s 进度条填充，与自动跳题定时器同步）
- **答对反馈**：发音波浪位置切换为绿色对勾徽章（浅绿底 + 弹入 + 一次性扩散圆环 + 对勾描画动画）；状态文案"回答正确"；下方 `+N 积分` pill（pointsForEntry 按词条类型 5/8/10 分）；释义改白色圆角卡片（音标+中文，英文留给拼写行绿色字母避免重复）；底部"上箭头+即将进入下一个"替换为细进度条自动填充 + "正在进入下一题 · 点击跳过"
- **揭示答案（我不会/拼错5次）**：琥珀色书本徽章（温和不打击）；文案改口语化（"多练几遍，马上就记住啦"/"没关系，看一遍答案"）；释义卡片 amber 边框浅暖底
- 布局稳定性：提示区 min-h 44→76px 适配卡片高度；徽章区固定 h-24 与波浪等高避免跳动；序号 tabular-nums
- 验证：tsc 无错；dev server HMR 生效

## 英式发音音频本地化（全部随包分发）

- 用户决策：默认改为外研社+英式后，英音也本地化（有道英音生成，随包分发）
- 新脚本 `scripts/generate_audio_uk.mjs`：读取 manifest.json（1601 去重文本）→ 有道 dictvoice type=1（英音）下载 → 输出 `{id}-uk.mp3` + `manifest-uk.json`；断点续传（已有清单跳过）；8 分钟完成
- 结果：**1531/1601 成功（95.6%）**，70 条失败（HTTP 500，全为短语/句子——有道英音词典库只收单词），失败清单 `scripts/audio_uk_failed.log`；失败项自动回退本地美音
- `audio.ts` 英式链路已完善：本地英音（manifest-uk.json，`{id}-uk.mp3`）→ 有道英音 type=1 → 本地美音 → 有道美音 → 百度；`ensureLocalManifest(variant)` / `resolveLocalAudio(text, variant)` 双清单
- 体积：英音 41.5MB；总音频 72.3MB（美音 2064 + 英音 1531 = 3595 个 MP3），dist 81MB
- 构建：沙箱下清空 dist（3595+ 文件）会 rolldown 报错，提权（escalation）后成功；dev server 英音文件 200

## 英音缺失补全（有道英音库无短语/句子 → 微软 Edge TTS 补全）

- 用户反馈"英音少500多个"：实为美音文件 2064（按词条id存，重复文本多份）vs 英音 1531（按去重文本存，文本级覆盖）的文件数差；真正缺失仅 70 条（有道英音词典库不收录的短语/句子，HTTP 500）
- 补全方案：微软 **edge-tts**（免费无 key，en-GB-SoniaNeural 英音女声），pip 从清华镜像安装（pypi.org 连接超时）；新建 venv `C:/Users/huawei/.workbuddy/binaries/python/envs/default`
- 新脚本 `scripts/fix_audio_uk.py`：读 manifest.json 与 manifest-uk.json 求差集 → edge-tts 生成 `{id}-uk.mp3` → 回写清单；5 并发，70 条 47 秒全部成功
- 结果：**英音 1601/1601（100% 覆盖三套词库全部去重文本）**；文件 1601 个 42.4MB；总音频 3665 个 73.2MB；dist 82M
- 验证：两份清单 0 缺失；构建通过（提权后台失败→前台重跑成功模式再现）；dev server 补全文件 200
- 注意：补全项音色为 Sonia Neural 合成音，与有道真人英音略有差异（仅影响 70 条短语/句子）

## 音频本地覆盖核验（三套教材美音/英音 100%）

- 新脚本 `scripts/verify_audio_coverage.mjs`（可复用）：正则解析三套词库 mk 调用重建 id（人教全局 seq / 外研·牛津 prefix 独立 seq）→ 比对 manifest.json / manifest-uk.json → 校验 manifest 引用文件存在性
- 结论：2980 词条（人教 1060 + 外研 960 + 牛津 960）去重后唯一文本 1601 个，美音/英音各 1601/1601 全覆盖，manifest 引用 mp3 0 缺失，完全离线可用
- 文件口径：美音 2064 个 mp3（人教 1060 + 外研 389 + 牛津 615，跨版本重叠文本只存一份、manifest 复用）+ 英音 1601 个 = 3665 mp3（另 2 个 manifest json）
- 外研/牛津美音文件数 < 词条数是设计行为（generate_audio_editions.mjs 的 reused 逻辑），非缺失

## 设置页文案清理 + 进度行为确认

- `SettingsPage.tsx` 使用说明删除过时文案"外研社与沪教牛津词条的音频会在第一次学习时联网获取并缓存到本机，之后可离线使用"（音频已全部本地化，此提示不再适用）；保留"切换教材后进度从第一单元开始"说明；tsc 通过
- 确认进度行为（未修改）：点"我不会"仅加入 difficultEntryIds（重点记忆），不加分、不进 completedEntryIds；答对才进 completedEntryIds 并加分；重点记忆模式答对同样走 markComplete → 计入完成进度 + 加分，且答对后词条仍保留在重点记忆列表

## 首页设置按钮改动（尝试后回退）

- 曾把 HomePage 设置按钮改为文字胶囊"⚙️ 外研社/英式"（含 title 提示），用户反馈"太占地方"后**回退为纯齿轮图标**（36px 圆形，保留 title="点击选择教材和读音"）；tsc 通过

## 学习页空格键查看提示

- `LearningCard.tsx` 新增 keydown 监听：空格键（e.code === "Space"）切换查看提示（toggle showHint），仅未完成且未揭示时生效；preventDefault 阻止空格滚动页面；tsc 通过
- **坑**：SpellingInput 有自动聚焦的隐藏透明 input（opacity-0、1x1、pointer-events-none），第一次实现按 `tag === "INPUT"` 一律跳过导致空格无效；修正为仅跳过可见 input（`getComputedStyle(t).opacity === "0"` 判定隐藏输入框放行），按钮/TEXTAREA/contentEditable 仍跳过；另加 `e.isComposing || e.keyCode === 229` 防中文输入法选词误触

## 学习页输入框焦点常驻

- `SpellingInput.tsx` 新增 document 级 click 监听：答题中（!done && !revealed）点击页面任意位置（提示卡片、按钮、空白处）后自动 `inputRef.focus()` 恢复焦点；按钮 onClick 先于 document 冒泡执行故不影响按钮功能；revealed/done 时不抢焦点；tsc 通过
- 与空格键查看提示的兼容性：焦点常驻隐藏 input 后，按空格 e.target 即隐藏 input（判定放行）→ 触发提示，功能不受影响且更稳定

## 清空学习进度：积分一并清零 + 警示弹窗

- `App.tsx` handleReset：resetProgress 后新增 points=0、learnedCount=0（commitUsers 持久化 + setCurrentUser 同步刷新）
- `HomePage.tsx`：原生 confirm 替换为自定义警示弹窗（红色警示区 ⚠️ 显示"全部积分（⭐ N 分）将一并清零"，红底 #FDECEB、红色确认按钮 #E24B4A），遮罩点击/取消关闭；tsc 通过

## 年级完成祝贺页 + 100 条电影台词库

- 新文件 `src/data/movieQuotes.ts`：MOVIE_QUOTES 100 条（en?/cn/movie，英文经典为主+5条中文电影台词：喜剧之王/少林足球/哪吒/流浪地球/中国合伙人），randomMovieQuote() 随机抽取
- `progress.ts`：Progress 扩展 `gradeStartedAt?: Record<grade,timestamp>`、`mistakeEntryIds?: string[]`、`celebratedGrades?: number[]`（freshProgress 初始化 + loadProgress 兼容补全）
- `SpellingInput.tsx`：新增 `onFirstMistake` 回调（首次拼错字母时触发一次）
- `LearningCard.tsx`：新增 `onMistake` prop 转发；revealAnswer（我不会/拼错5次）也调 onMistake
- `LearnPage.tsx`：
  - lastCompletedGradeRef 记录最近完成词条的年级（markComplete 内设置，避免切题竞态）
  - effect：首次进入某年级记录 gradeStartedAt；completedEntryIds 变化时检测该年级全部完成 → 标记 celebratedGrades（持久化防重弹）+ setCelebration + speech.stop()
  - 祝贺页 z-40 全屏渐变（紫→米白）：🎉 恭喜完成 X 年级、6 项统计（开始学习时间/完成用时/拼错或不会词条数/一次做对词条数/本年级积分/完成词条数）、随机台词卡片（英文+中文+电影名）、继续学习/返回首页按钮
  - 格式函数 formatDateTime / formatDuration（文件级）
- 数据持久化：App.tsx setProgress 已封装 saveProgress，新字段自动落盘；tsc 通过
- 测试提示：年级已全部完成但无庆祝记录的旧进度，再答对该年级任意一题即触发祝贺页

## 祝贺页改为"每单元完成都触发"（从年级粒度改为单元粒度）

- `progress.ts`：`gradeStartedAt` → `unitStartedAt`（key `${grade}-${unit}`）、`celebratedGrades: number[]` → `celebratedUnits: string[]`；loadProgress 兼容迁移：旧 celebratedGrades → 该年级所有单元 key 视为已庆祝；旧 gradeStartedAt → 复制到该年级各单元
- `LearnPage.tsx`：lastCompletedGradeRef → lastCompletedUnitRef（存 `${grade}-${unit}`）；检测 effect 改为按单元 entries 全完成才弹；统计 useMemo 按单元（mistake/points/startAt/duration）；标题"恭喜完成 X 年级第 N 单元！"+ 单元 title；副标题/统计文案相应改"本单元"；tsc 通过
- 注意：celebratedUnits/unitStartedAt 每个单元独立，学完每个单元都会弹一次祝贺页（每单元只弹一次）

## BUG 修复：祝贺页弹出后播放下一题音频 + 新增庆祝音效

- **根因**：答对最后一题时 frozen 尚未置位，LearningCard 的 autoNextTimer（1.8s）已排队 → 祝贺页弹出后仍自动切题并 startAudio 下一题；原 speech.stop() 拦不住新起的音频
- **新建 `src/lib/celebration.ts`**：`playCelebrationJingle()` —— Web Audio API 合成 C5-E5-G5-C6 上行琶音 + 结尾 C 大调和弦（~1.5s），纯本地无资源文件，失败静默降级
- **`LearningCard.tsx`**：新增 `frozen` prop；frozen 时 entry effect 跳过 startAudio、handleComplete 不设 autoNextTimer、空格不响应；**关键竞态修复**：frozen 变 true 的 effect 清除已排队的 autoNextTimer
- **`LearnPage.tsx`**：celebration effect 调 playCelebrationJingle()；LearningCard 传 frozen={celebration!==null}；庆祝页打开时屏蔽 Enter/Escape（键盘监听）与上滑手势（handleTouchEnd）；"继续学习"按钮 = setCelebration(null) + goNext() 无缝进下一题；tsc 通过
- **BUG：点"我不会"导致单元祝贺页不弹（已修）**。根因①单元完成判定 only 看 completedEntryIds，"我不会"只进 difficultEntryIds → every() 永远 false；根因②lastCompletedUnitRef 只在 markComplete 更新，最后一个词条走"我不会"时不触发检测。修复：判定改为 completed ∪ difficult 全覆盖（"全部处理过一遍"即完成），ref 改名 lastProcessedUnitRef 并在 markDontKnow 也更新；检测从数据驱动 effect **迁移到 goNext 内**（普通模式分支），保证"我不会"揭示答案后用户先看完答案、点"记住了"/Enter/上滑离开时才弹；celebratedUnits 防重复标记不变，"继续学习"→goNext 正常切题；祝贺页文案"全部完成"→"全部学完"（口径含不会的词条）；tsc 通过

## 祝贺页分级展示：单元=只祝词+台词，年级=完整统计

- `celebration` 状态加 `level: "unit" | "grade"`；goNext 检测到单元全部处理后，再检查该年级所有词条是否也处理完（completed ∪ difficult）→ 是则 level="grade"（年级最后一个单元的单元级祝贺被年级级吸收，celebratedUnits 标记照常写入，不重复弹）
- **单元级**：🎉 + "恭喜完成 X年级第N单元！" + 单元主题副标题 + 台词卡片（无统计卡片，台词卡片 mt-6 补位）
- **年级级**："恭喜通关 X年级！" + "N 个单元、M 个词条全部学完"副标题 + 统计六宫格（开始学习=该年级各单元 unitStartedAt 最小值/完成用时/拼错或不会/一次做对/本年级积分/完成词条）+ 台词
- 统计 useMemo 改名 unitCelebrationStats → celebrationStats，union 类型按 level 区分字段（unit 分支含 unit/title；grade 分支含 unitCount/doneCount/mistakeCount/onceRight/gradePoints/startAt/durationMs）；tsc 通过
