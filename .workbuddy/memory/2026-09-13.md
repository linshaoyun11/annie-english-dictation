
## Ms 逐字母拼读修复（build 96）

- 用户实测 `Ms` 读字母（9-12 的全库扫描漏网）。确诊有道+Edge 对称谓词
  Ms/Dr 默认都逐字母；mr/mrs/dr英 整读正常。
- 修 4 文件同名覆盖：ms 美/英→Edge "Miz"；dr 美、dr. 美→Edge "doctor"。
  备份 .workbuddy/baselines/title-before-fix/。
- 判据教训（已记 audio-pipeline.md）：时长比/间隙/互相关三判据对短称谓词
  全有模糊区，同音词绕道（Miz）+ 用户耳朵是金标准。
- 波及：仁爱线 13 条 `*星号` 词条（含 *Ms）去星号，id 不变无需升 CV，
  全部命中现有音频；tsc 过。

## 后台唤醒自动跳题修复（build 97，第四路：Worker 心跳）

- 用户实测：唤醒后自动跳题仍失效 ⇒ build 92/93 三路在唤醒场景全灭。
  **唤醒冻结连 rAF 和 animationend 派发一起杀**（进度条合成器动画仍在走
  但主线程渲染更新停摆），"合成器活着⇒rAF活着"前提被证伪。
  唯一确认存活的通道：用户输入事件。
- 修复：`src/lib/workerHeartbeat.ts` —— Blob Worker 独立线程 100ms 心跳
  postMessage，主线程数拍（ceil(ms/100)+1 防相位提前），构造失败退回原生
  setTimeout。LearningCard 答对页新增第四路 effect，fireAutoNext 四路互斥。
- 冒烟：无头 Edge Blob Worker 19 拍 ≈1916ms ✓（.workbuddy/tmp/worker_smoke.cjs）。
- ⚠️ 未真机验证项：唤醒后 Worker 消息是否可达（与输入事件同族通道，
  推断可达）；整个进程挂起期间 Worker 同停、唤醒后恢复计数（略晚跳，可接受）。

## 通关页按钮顺序调整（build 98）

- 单元通关页："继续学习"（主按钮）移到"重新学习本单元"上面；间距类
  随位置互换（mt-2.5 移给次按钮）。tsc 过。
