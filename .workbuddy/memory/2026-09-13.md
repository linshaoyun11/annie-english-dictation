
## Ms 逐字母拼读修复（build 96）

- 用户实测 `Ms` 读字母（9-12 的全库扫描漏网）。确诊有道+Edge 对称谓词
  Ms/Dr 默认都逐字母；mr/mrs/dr英 整读正常。
- 修 4 文件同名覆盖：ms 美/英→Edge "Miz"；dr 美、dr. 美→Edge "doctor"。
  备份 .workbuddy/baselines/title-before-fix/。
- 判据教训（已记 audio-pipeline.md）：时长比/间隙/互相关三判据对短称谓词
  全有模糊区，同音词绕道（Miz）+ 用户耳朵是金标准。
- 波及：仁爱线 13 条 `*星号` 词条（含 *Ms）去星号，id 不变无需升 CV，
  全部命中现有音频；tsc 过。

## 后台唤醒自动跳题修复（build 97，第四路：Worker 心跳）

- 用户实测：唤醒后自动跳题仍失效 ⇒ build 92/93 三路在唤醒场景全灭。
  **唤醒冻结连 rAF 和 animationend 派发一起杀**（进度条合成器动画仍在走
  但主线程渲染更新停摆），"合成器活着⇒rAF活着"前提被证伪。
  唯一确认存活的通道：用户输入事件。
- 修复：`src/lib/workerHeartbeat.ts` —— Blob Worker 独立线程 100ms 心跳
  postMessage，主线程数拍（ceil(ms/100)+1 防相位提前），构造失败退回原生
  setTimeout。LearningCard 答对页新增第四路 effect，fireAutoNext 四路互斥。
- 冒烟：无头 Edge Blob Worker 19 拍 ≈1916ms ✓（.workbuddy/tmp/worker_smoke.cjs）。
- ⚠️ 未真机验证项：唤醒后 Worker 消息是否可达（与输入事件同族通道，
  推断可达）；整个进程挂起期间 Worker 同停、唤醒后恢复计数（略晚跳，可接受）。

## 通关页按钮顺序调整（build 98）

- 单元通关页："继续学习"（主按钮）移到"重新学习本单元"上面；间距类
  随位置互换（mt-2.5 移给次按钮）。tsc 过。

## 六线专有词条大清理（build 99，CV 28→29）

- 用户报人教三起有人名（Helen），要求六线全清人名/地名/缩写/电影名。
- 扫描：scan_proper3.py（兼容 mk 6/7 参与 w() 三种调用；课标表对照标必留）。
- 删除 215 条，全在 grades4to9(210)+waiyanshe(5)+renai(5)：
  人名 ~85（Helen/Ella/Emma/…/Mark Twain/莎士比亚/安徒生/毕加索/J.K.罗琳等），
  地名/景点/机构 ~95（Sydney/Australia/巴黎/金字塔类/Blue Sky Rescue/WildAid 等），
  缩写 11（PRC/PLA/VR/UN/UK/AM/PM/WWF/RSVP/ID×2/GPS/B.C./A.D.），
  书/电影/歌名 22（西游记/哈利波特/三体/老人与海/友谊地久天长/泰坦尼克号等）。
- 保留（判定依据）：课标必留（Brown/Green/Wood/Bill/Rose/WHO/US/IT/AI/X-ray/
  T-shirt/TV/PE/a.m./p.m./China/Miss/Sir/Dear）；句子型条目全保留（含人名的
  教材句型）；行星天体（Mars/Venus 等太阳系单元词汇）；国籍语言
  （Finnish/Thai/Arab 等）；食物名（宫保鸡丁/东坡肉）；节日；
  the Olympics/the Red Army/Nazi/WWII/Pulitzer Prize。
- 脚本坑：G_SPECIAL 元组混进字符串集合导致 9 条未命中，二轮补删（教训：
  匹配逻辑逐类验证，勿混类型）。America 首轮漏网，三轮补删。
- SL 三线复扫 0 新增（此前已清）。CV 29 触发 freshProgress（积分保留）。
- 备份：.workbuddy/baselines/proper3fix/。
