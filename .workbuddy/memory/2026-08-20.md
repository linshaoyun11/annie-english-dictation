# 2026-08-20

## 修复：单元完成祝贺页在"答对最后一题自动跳题"路径下不弹出

- 现象：单元最后一题**答对**后（1.8s 自动跳题）祝贺页不弹；"我不会"路径正常
- 根因：LearningCard `handleComplete` 里 `onComplete`（setProgress 更新 completedEntryIds）与 `autoNextTimer` 属同一次渲染闭包，定时器 1.8s 后调用的是**旧 `goNext`**，其闭包中的 `progress` 不含刚答对的最后一题 → `allProcessed` 恒为 false → 直接切下一单元，单元被跳过庆祝（celebratedUnits 未标记）
- 修复（`src/pages/LearnPage.tsx`）：新增 `progressRef`（每次渲染同步 `progressRef.current = progress`），`goNext` 内改读 `progressRef.current`（完成判定 + entryIndex/unitIndex 推进全部用 p），`useCallback` deps 移除 `progress`
- 经验：凡是被 setTimeout 定时器延迟调用的回调，不能依赖闭包里的 state，必须用 ref 读最新值（本 app 第三次踩闭包过期坑：焦点、frozen 竞态、本次）
- 验证：`npx tsc -b --noEmit` 通过，Vite HMR 生效

## UI 整体改版（紫色主题 · 简洁趣味）

- 备份：`C:\Users\huawei\WorkBuddy\2026-08-17-22-58-27\.workbuddy\backups\ui-before-2026-08-20.tar.gz`（包含 src、index.html、configs）
- 设计系统（`src/index.css` 新增 `@theme`）：
  - 主色 `--color-primary: #534ab7`；浅紫 `--color-primary-light: #edebff`、`--color-primary-lighter: #f4f2ff`
  - 背景 `--color-bg: #f8f7ff`；卡片 `--color-surface: #ffffff`
  - 文字 `--color-text: #1f1d2e` / `--color-text2: #6b6780` / `--color-text3: #9b98ad`
  - 语义色 `--color-success` / `--color-error` / `--color-gold`
  - 阴影 `--shadow-card`、`--shadow-button`
- 页面级改动：
  - `HomePage`：顶部改为头像+积分+三个圆形图标按钮；当前进度卡片更突出；年级卡片加 mini 进度条；清空弹窗升级为圆角卡片
  - `LearnPage` / 祝贺页：统一字体/卡片/按钮规范；统计卡用 `StatCard` 组件
  - `SettingsPage`、`LeaderboardPage`、`DifficultWordsPage`、`RegisterPage`、`UserSelectPage`：统一卡片、按钮、图标、阴影
  - `PasswordModal`：更大圆角、统一色板
- `LearningCard` 答对状态大改（截图中的凌乱问题）：
  - 原先分散的「✓ + 回答正确 + 积分 + 释义卡 + 字母下划线 + 进度条」合并为单张成功卡片
  - 成功页：大绿色徽章 + 英文单词大标题 + 音标/释义 + 积分 tag + 底部「下一题」按钮（自带倒计时进度条）
  - 揭示页：温暖卡片 +「已加入重点记忆」+ 答案 + 下一题按钮
  - 答题页：更大的紫色声波按钮、精致「查看提示」胶囊、更大的字母格
- `SpellingInput`：字母格更宽松、当前位下划线用主紫色、非当前位用淡紫灰

## 注册页头像选择区滚动条修复

- 问题：选中头像后选中标记（✓）超出按钮边界，触发容器横向/纵向滚动条
- 修复（`src/pages/RegisterPage.tsx`）：
  - 头像从 `h-14 w-14 text-[30px]` 缩小到 `h-12 w-12 text-2xl`
  - 按钮内边距从 `p-2` 缩到 `p-1.5`，纵向间距 `gap-y-4`→`gap-y-3`
  - 选中标记改为 `right-1 top-1 h-4 w-4`，收在按钮内部不外溢
  - 头像区容器增加 `overflow-x-hidden` 防止标记溢出产生横向滚动
- 验证：`npx tsc -b --noEmit` + `npx oxlint` 0 errors
- `SoundWave`：更大、淡紫背景、圆角胶囊
- `index.html` theme-color 同步改为 `#F8F7FF`
- 验证：`npx tsc -b --noEmit` 通过；`npx oxlint` 0 errors

## 新增「答对后是否自动进入下一题」设置项

- 需求：设置页可切换；默认关闭（答对后停在正确页，按空格或点按钮继续）；开启时沿用原 2s 自动跳题
- 实现：
  - `UserConfig` 新增可选字段 `autoNext?: boolean`，`DEFAULT_CONFIG.autoNext = false`（`src/lib/users.ts`）
  - `SettingsPage` 增加「答题行为」区块 + 开关卡片，`commit` 合并 autoNext（`src/pages/SettingsPage.tsx`）
  - `LearningCard` 新增 `autoNext` prop：`handleComplete` 仅在 `autoNext` 时起 2s 定时器；进度条仅在 `completed && autoNext` 时显示；正确页提示文案随开关切换（`src/components/LearningCard.tsx`）
  - 空格键行为扩展：答对 / 揭示答案后按空格 → 清定时器并 `onNext()`（答题中仍为切换查看提示）
  - `LearnPage` 传 `autoNext={user.config.autoNext ?? false}`
- 验证：tsc + oxlint 0 errors

## 小调整：答对页去重 toast + 自动跳题改为 2 秒

- 用户反馈：答对页上方卡片已有 +5 积分标签，下方 toast 再提示 +5 积分造成重复，且 1.8 秒自动跳题太快。
- 调整（`src/components/LearningCard.tsx`、`src/pages/LearnPage.tsx`）：
  - 移除 `markComplete` 里的积分 toast，答对不再弹底部 `+N 积分` toast；「已加入重点记忆」toast 保留
  - 自动跳题延迟从 1800ms 改为 2000ms；进度条动画、提示文案同步改为 2 秒
- 验证：`npx tsc -b --noEmit` 通过；`npx oxlint` 0 errors

## 小修 + 新增：学习页顶部重合 & 手机键盘避让

- 修复：学习页右上角关闭按钮与顶部「年级·单元/词条标签」重合 → `LearningCard` 根容器 `pt-5` → `pt-14`
- 新增键盘避让（用户问手机键盘会不会挡输入框，答案是会，尤其 iOS）：
  - `LearnPage` 新增 `visualViewport` 监听：键盘高度 = `innerHeight - vv.height - vv.offsetTop`（>60px 视为键盘弹出），写入 CSS 变量 `--kb-h`
  - 学习页根容器由 `h-full` 改为 `style={{ height: "calc(100% - var(--kb-h, 0px))" }}`，键盘弹出时高度压缩、居中内容自动上移到键盘上方
  - 桌面端 kb 恒为 0 不受影响；为将来 Capacitor 打包铺路
- 验证：`npx tsc -b --noEmit` 通过

## 布局调整：关闭按钮与顶部进度信息同行

- 用户要求关闭按钮和顶部进度条放同一行
- `LearningCard`：新增 `onExit` prop，关闭按钮（× 圆形）移入顶部信息行右侧（与 ScorePill 并排）；根容器 `pt-14` 恢复为 `pt-5`
- `LearnPage`：删除绝对定位（top-3 right-3）的退出按钮，`onExit` 传给 LearningCard
- 验证：`npx tsc -b --noEmit` 通过；oxlint 0 errors

## 首页：移除 Logo 区，内容上移

- 用户要求去掉首页上方带耳机图标的 Logo 标题区（“安妮英语听写 / 外研社·小学至初中”）
- `HomePage`：删除 Logo 区 JSX 和 `Logo`、`CURRICULUM_LABELS` 导入，当前学习进度卡片直接跟在顶部导航下方
- 追加修复：Logo 区删除后进度卡片与顶部头像导航行间距缺失（视觉重叠），给进度卡片加 `mt-6`
- 验证：`npx tsc -b --noEmit` 通过；oxlint 0 errors

## 台词库精选：只保留励志奋进台词

- 用户要求电影台词只挑励志、催人奋进的
- `src/data/movieQuotes.ts`：从 100 条精简到 84 条，删除跑题台词（及时行乐译法、巧克力人生、Hakuna Matata、责任越大、原力同在、阿拉丁情歌、冰雪奇缘爱情 2 条、绿野仙踪思乡、乐高超棒、帕丁顿礼貌、小熊维尼温情、终局之战终点论、神奇女侠、海洋奇缘 1 条等），保留上进/坚持/勇气/梦想主题；「Carpe diem」译法改为「把握当下，抓住今天」
- 验证：tsc + oxlint 通过

## 课标 1600 词补全：数据生成 + TS 填充 + 三教材接入 + 音频本地化

- 数据管线（`.workbuddy/kebiao_gen.py`）：ipa-dict en_UK.txt 音标 + ECDICT 释义 + OVERRIDE/IPA_FIX/CN_FIX 人工覆盖 → 1619 条 {en, ph, cn, band}
  - normalize_ipa 四轮迭代：ɐ→ə、ɹ→r、ɛ→e；iə→ɪə；重音移到合法音节首辅音簇前（VALID_ONSET2/3 白名单）；ː 长音符处理；单音节去重音；非复合词去词中次重音 ˌ；词尾 r/re 时 ə→ə(r)、ɔː→ɔː(r)
  - clean_cn：a.→adj.、vt./vi.→v.、去 `[xx]` 领域前缀、取第一义项 ≤20 字
  - CN_FIX 约 300 条：修正 ECDICT 首义项偏差（coach→教练、dumpling→饺子、mum→妈妈、tap→水龙头、tidy→整洁的、still→仍然、can→能、kid→小孩、live→住/活、plane→飞机 等）
  - IPA_FIX 约 100 条：75 个 en_uk.txt 缺失基本词 + 16 个 ex- 前缀词 + tour/museum/realise/medicine 等
  - 最终：1619/1619 全成功，0 释义缺词性，0 多音节缺重音
- TS 填充（`src/data/kebiaoBank.ts`）：KEBIAO_BANK 1619 条 + buildKebiaoUnits（band=2→3-6年级、band=3→7-9年级，每单元15词，字母序）+ insertKebiaoUnits + applyKebiaoTo 便捷封装
- 三教材接入：curriculum.ts / waiyanshe.ts / oxford.ts 末尾各调 applyKebiaoTo，insertKebiaoUnits 原地插入；CURRICULUM_VERSION 6→7
- 验证：tsc + oxlint 通过，0 errors
- 音频本地化：`scripts/generate_audio_kebiao.mjs` → 766 词 × 2 口音 = 1532 文件，全部下载成功（0 失败）
  - 美音：有道 type=2 + 百度 TTS 兜底 → `public/audio/kb{seq}.mp3`
  - 英音：有道 type=1 → `public/audio/kb{seq}-uk.mp3`
  - manifest.json 1601→2367 键，manifest-uk.json 同步
- 最终 APP 大小：dist/ = 115 MB（音频 114 MB / 5197 文件 + JS 544 KB + CSS 36 KB + HTML 1.3 KB）

## 发布前全面测试（5 项全通过）

1. **构建验证**：tsc 0 错误 / oxlint 0 错误（4 警告均在 scripts/ 不影响 app）/ 生产构建 2.94s 成功
2. **数据完整性**：CURRICULUM_VERSION=7 ✓ / KEBIAO_BANK 内 0 重复 ✓ / 三教材 1-9 年级全覆盖 ✓ / 课标补全人教+1135 / 外研+1125 / 牛津+1051
3. **音频覆盖**：美音 2367/2367 (100%) ✓ / 英音 2367/2367 (100%) ✓ / 文件全部存在且非空 ✓
4. **运行时冒烟**：dev server HTTP 200 ✓ / curriculum.ts+main.tsx+App.tsx 编译无错误 ✓ / 音频抽样 US+UK 全可访问 ✓
5. **核心逻辑**：旧 v6 进度自动重置（积分保留）✓ / 积分系统独立存储 ✓ / 音频按文本映射（跨教材共享）✓ / 课标 1619 词（1615 单词 + 4 短语）✓

## 修复：单元完成祝贺页后继续学习偶发回到上一单元末词

- 现象：用户反馈，单元学完弹出祝贺页后，点「继续学习」有时会回到刚完成单元的最后一个单词（10/10）
- 根因：`goNext` 依赖 `progressRef.current` 与 `lastProcessedUnitRef.current` 的时序；祝贺页关闭时直接调用 `goNext()`，存在竞态，闭包/Ref 读到旧状态导致推进异常
- 修复（`src/pages/LearnPage.tsx`）：
  - 新增 `busyRef` 防止 `goNext` / 继续按钮被快速重复触发
  - 祝贺页「继续学习」按钮改为**显式推进进度**（不调用 `goNext`），基于当前 `progress` 状态直接计算下一题/下一单元，并清空 `lastProcessedUnitRef.current`
  - `goNext` 内部所有返回路径均释放 `busyRef`
- 验证：`npx tsc -b --noEmit` 通过；`npx oxlint` 0 errors（仅 scripts/ 4 个既有警告）

## 学习页顶部显示单元号 + 年级卡片/重复词条问题排查

- 需求 1：学习页顶部显示当前是第几单元
  - 修改 `src/pages/LearnPage.tsx`：传入 `LearningCard` 的 `unitTitle` 改为 `第 ${unit.unit} 单元 · ${unit.title}`
  - 显示效果：`小学1年级 · 第 1 单元 · School 学校`
- 需求 2：核实学习进度推进是否导致同一词条重复出现
  - 写 SSR 测试脚本 `.workbuddy/test_learn_flow.mjs`：模拟完成单元 0 全部词条，验证 `unitOrder` 内无重复 ID，相邻单元无重叠词条
  - 结果：单元内无重复，单元 0→1 重叠 0 条，前 5 单元无跨单元重复英文词
  - 结论：当前代码推进逻辑正常；用户感知的"重复"可能是旧缓存/旧版本 bug，或同一英文词跨年级复习设计
- 需求 3：年级小卡片进度显示不对
  - 截图显示 1 年级 `10 单元 / 100 词`，但实际代码中 1 年级为 `12 单元 / 120 词`，3-9 年级也包含课标补全单元
  - SSR 检查 `check_curriculum.mjs`：人教总单元 185、总词条 2195；1/2 年级各 12 单元 120 词，3-9 年级含课标单元
  - 结论：截图来自浏览器缓存的旧代码；需强制刷新（Ctrl+F5 / Cmd+Shift+R）加载最新代码
- 验证：`npx tsc -b --noEmit` 通过

## 修复：SpellingInput 复合词/连字符解析错位

- 现象：用户截图，学习页 `man-made`（人造的）输入区只显示 `- made`，位置错位
- 根因：`SpellingInput.parseTarget` 扫描到第一个非字母字符就停止，把 `-made` 整体当作 `suffix` 塞进一个 22px 的小格子里，文本溢出导致布局错乱
- 修复（`src/components/SpellingInput.tsx`）：重写 `parseTarget`，按字母段/非字母段交替切分，例如 `man-made` → `[man, -] + [made]`，连字符/撇号作为短 suffix 显示在对应 group 后
- 验证：`npx tsc --noEmit` 通过，`npm run build` 成功

## 新增：年级独立进度保存与恢复

- 需求：每个年级的进度独立保存，点首页年级卡片能接着该年级上次的进度继续学（原行为：总是从年级第一单元第一题开始，且覆盖全局指针）
- 实现：
  - `src/lib/progress.ts`：`Progress` 新增 `gradeProgress?: Record<string, GradeProgress>`（key=年级号，存 unitIndex/entryIndex/unitOrder）；`loadProgress` 兼容旧数据——无此字段时把全局当前位置迁移为该年级进度；`freshProgress` 初始化空对象
  - `src/pages/LearnPage.tsx`：新增同步 effect，unitIndex/entryIndex 变化时自动把当前位置写入 `gradeProgress[当前年级]`（difficultMode 不同步；所有推进路径统一落盘，无需逐点保存）
  - `src/App.tsx` `startLearning(unitIndex)`：点年级卡片时优先恢复 `gradeProgress[grade]`；恢复时跳过已完成题（答对未切题退出、单元学完场景）；单元全部完成则 entryIndex 回 0 复习；保存位置不属于该年级的脏数据不恢复
  - 「继续学习」主按钮（无参）行为不变：从全局指针（最后学习位置）继续
- 验证：tsc + oxlint 0 错误；`npm run build` 成功；SSR 脚本 `.workbuddy/test_grade_progress.mjs` 10 项断言全通过（fresh 初始化 / 旧数据迁移 / 多年级独立保存 / 跳过已完成题 / 全完成回 0 / 脏数据防护）

## 新增：祝贺页空格键继续学习

- 需求：单元/年级完成祝贺页弹出时，按空格键等同点「继续学习」
- 实现（`src/pages/LearnPage.tsx`）：
  - 把「继续学习」按钮的 onClick 逻辑抽成 `continueFromCelebration` useCallback（按钮与键盘共用）
  - keydown 监听：celebration 打开时空格触发它；焦点在 BUTTON/INPUT/TEXTAREA 上时跳过全局监听（让空格走原生按钮激活，避免重复触发）；busyRef 防重入沿用
  - LearningCard 的空格监听在 frozen 时已屏蔽，无冲突
  - 按钮下新增「按空格键也可继续」提示文案
- 验证：tsc + oxlint 0 错误；`npm run build` 成功（首次失败为 dist 清理偶发超时，重跑通过）

## 单元词条数疑虑核实 + 重点记忆列表倒序

- 用户担心每单元随机取 10 词导致学不完全部词条 → 写 `.workbuddy/check_unit_sizes.mjs` 核实：三教材单元词条数为教材数据本身决定（人教 106 个 10 词单元 + 72 个 15 词课标单元等），`makeUnitOrder` 打乱单元全部词条、无截断，总进度可 100%；用户确认不加设置，保持现状
- 重点记忆列表按加入时间倒序（`src/pages/DifficultWordsPage.tsx`）：`difficultEntryIds` 追加序即加入时间序，展示时 `slice().reverse()`，最新加入的排最前；学习顺序与存储不变
- 验证：tsc + oxlint 0 错误

## 设计：安妮英语听写 App 图标方案

- 共生成 3 轮图标：v1 童趣卡通（用户反馈太低龄）→ v2 现代几何无文字 → v3 带品牌名「安妮英语听写」
- v3 方案（4 个）：紫色渐变 + 磨砂玻璃质感 + 中文名横排，适合小学到初中
  1. 声波字母 A + 安妮英语听写
  2. 极简耳机 + AB 字母块 + 安妮英语听写
  3. ANNE 字母块 + 安妮英语听写
  4. 打开的书 + 对勾 + 安妮英语听写
- 文件位置：`app-icons/v3/`

## 多机型布局兼容性核实（全部通过）

- 方法：puppeteer-core + 系统 Edge headless（未下载浏览器），7 视口（360×640@2.75x ~ 1280×800@1x）× 8 页面全交互流程实测 + DOM 自动检测（横向溢出/元素越界/文本溢出/元素重叠）+ 祝贺页专项（答对末题→祝贺页→继续学习）
- 结果：56 项主流程全 ✓；文本溢出 0、布局重叠 0（唯一报告为 SVG 播放按钮 circle/path 正常绘制）；360×640 最小屏祝贺页高度恰好放下无需滚动
- 代码结论：max-w-md 居中 + flex/grid 自适应 + overflow-y-auto 滚动容器，架构本身对屏幕尺寸鲁棒；键盘避让 --kb-h 已生效
- 限制（非缺陷）：横屏、系统超大字体、iOS 安全区未适配（建议打包原生时补 `pb-[env(safe-area-inset-bottom)]`）
- 截图 60 张：`.workbuddy/screenshots/`（命名 `机型-页面.png`）；报告：`docs/layout-compatibility-report.md`
- 测试脚本（可复用）：`.workbuddy/layout_test.mjs`（主流程+截图+溢出检测）、`.workbuddy/layout_deep_test.mjs`（DOM 深检）、`.workbuddy/test_celebration_layout.mjs`（祝贺页专项，注意外研社 1 年级起点 → 预置 gradeProgress 用 key "1"）
- 经验：puppeteer-core 安装到 `C:\Users\huawei\.workbuddy\binaries\node\workspace`，用系统 Edge `C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe` 免下载浏览器；ESM 引 puppeteer 用 createRequire 指向绝对路径

## 小改动两则

- 截图复制到项目根目录可见的 `screenshots/`（`.workbuddy/` 是隐藏目录，用户文件管理器看不到）
- `LearningCard`「我不会」按钮改版：下划线文字链接 → 暖琥珀色胶囊按钮（border-[#F0D9B0] bg-[#FFF8EC] text-[#A06A1F]）+ help-circle 问号图标 + hover/active 按压反馈，与揭示答案暖色卡片呼应；验证 tsc/oxlint 0 错误、build 成功

## iOS App Store 发布准备（用户明确要走苹果市场首发，后续安卓）

- 交付物：`docs/ios-appstore-release-guide.md`（完整手册：8 阶段流程、成本时间表、行动清单）+ 对话内流程总览图
- 已核实的关键事实（2026-08）：①苹果 2026-04-28 起强制 Xcode 26 / iOS 26 SDK 构建，配套 Capacitor 8（SPM 替代 CocoaPods，Node 22+）②蜂窝下载上限 200MB，项目音频 114MB → IPA 约 120MB 无需拆包 ③账号个人/公司均 $99/年，企业 $299 不能上架 ④Kids 儿童类目需 parental gate + 禁第三方分析/广告；建议不加入，用「中小学英语」措辞（指南 2.3.8 禁"For Kids"字样）⑤隐私标签可申报「不收集数据」（数据全本地）
- 项目现状：**未集成 Capacitor**（package.json 无相关依赖）；图标候选在 `app-icons/v3/`（4 个，待用户选定）；截图仅有浏览器版（screenshots/），正式提交需 Mac 模拟器重截
- 最大卡点：**用户无 Mac**（Windows 环境）——已给三方案：云 Mac（MacStadium/AWS/腾讯云，约 $40-80/月）、CI 云构建（Codemagic/Bitrise/GitHub Actions macOS runner）、代打包服务；长期建议二手 Mac mini
- 待办（用户侧）：注册 Apple Developer、申请软著（安卓强制）、选图标、写描述/关键词、托管隐私政策+支持页
- 经验：Bundle ID 创建后终身不可改，需先定（建议 com.annie.dictation）；App Store 截图规格 6.7"(1290×2796) + 6.5"(1242×2688)

## 发布准备：图标 + 隐私政策 + Capacitor Windows 侧集成

- 图标：用户选定原耳机声波图标（紫色背景 + 白色耳机 + 绿色声波），已用 AI 图生图生成高清 1024×1024 版本 `assets/High_resolution_app_icon__1024_2026-08-20T14-58-46.png`，并导出 `assets/app-icon.png`、`app-icon-1024.png` 及 iOS 常用尺寸（20/29/40/58/60/76/80/87/120/152/167/180）
- 隐私政策：起草完成
  - Markdown 版：`docs/privacy-policy.md`（中文）、`docs/privacy-policy-en.md`（英文）
  - 可部署静态页：`docs/privacy-policy.html`、`docs/privacy-policy-en.html`（响应式、紫色主题、含联系信息占位符）
  - 核心结论：不收集个人数据；本地存储学习记录；音频可能请求有道/百度 TTS（仅传英文文本）
- Capacitor Windows 侧集成：
  - 安装 `@capacitor/core@8.5.0` 与 `@capacitor/cli@8.5.0`（npm 因本地 trash 权限最终 exit 1，但包已实际写入 node_modules 且 `npx cap --version` 正常）
  - 已写入 `package.json` 依赖与 scripts（`cap:sync`、`cap:ios`、`cap:open:ios` 等）
  - 已创建 `capacitor.config.ts`：Bundle ID `com.annie.dictation`、appName `安妮英语听写`、webDir `dist`、iOS scheme/minVersion/contentInset 配置
  - 已更新 `.gitignore` 忽略 `ios/`、`android/` 原生平台目录
  - 已写 `docs/capacitor-ios-setup.md`：区分 Windows 已完成 vs Mac 必做步骤、Xcode 配置、TestFlight/上架流程、隐私标签填写、常见问题
- 验证：`npx cap config` 正常读取配置；`npx tsc -b --noEmit` 类型检查通过（仅 tsbuildinfo 写入权限报错，与代码无关）

