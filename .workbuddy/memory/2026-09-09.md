# 2026-09-09

## v23 升版：「3 年级起去重 1-2 年级已学词」机制废弃（推送完成）

### 用户原话
「设置页'三年起自动跳过已学词'的表述已经过时，请去掉」 → 「不要添加课标是说我们重新构建的词库」
（重新构建的 renjiao/waiyanshe/oxford 一线课标已经被基线吸收，叠加课标反而错位；
外研牛津仁爱这些尚未重建的线才需要课标补全。）

### 改了什么（4 文件 + 1 删）
| 文件 | 改动 |
|---|---|
| `src/data/curriculum.ts` | 删除 `rjEarlyWords` Set（约 line 1055）+ 删除 `RENJIAO_G1_START` 中的 filter 逻辑；同样删除 `wyEarlyWords` Set（约 line 1004）+ `WAIYANSHE_G1_START` 的 filter |
| `src/pages/SettingsPage.tsx:23-26` | 副标题：「1-9 年级，三年级起自动跳过已学词」→「1-9 年级，按新起点 SL 真实教材」/ 「按外研新标准真实教材」 |
| `src/lib/progress.ts:55-58` | lineTag 注释加 `2026-09-09（v23）已废弃 3 年级起去重机制` |
| `codemagic.yaml:28` | `APP_BUILD: 70 → 71` |
| `scripts/_audit_q.mjs` | 删（早期调试遗留） |

### 数据影响（升版触发 freshProgress）
| 线 | v22 | v23 | Δ |
|---|---:|---:|---:|
| renjiao | 2303 | **2410** | +107 |
| waiyanshe | 2602 | **2822** | +220 |
| renjiao3 / waiyanshe3 / oxford / renai | 不变 | 不变 | 0 |

旧过滤错删的 G3+ 课标必学词已恢复：
- renjiao：my / your / name / class / grade / age 等
- waiyanshe：weather / family / subject / around 等

### 其它三条线数据 0 变动
- renjiao3 (PEP 三起) 3971 词 = 之前
- waiyanshe3 (外研三起) 2348 词 = 之前
- oxford 1557 词 = 之前
- renai (科普仁爱) 2388 词 = 之前

### 推送
- commit `59be358` (HEAD, local & origin)
- `git push --force origin main` (用 /tmp/annie_key 直连 ssh.github.com:443, ~13 秒)
- 远端 `ls-remote origin main` = `59be358ba6987d57505b3dcbbf235fe76a0fcac1` ✓
- **Codemagic 自动构建**：监听 main branch push + cancel_previous_builds ⇒
  build 71 应已自动启动（按 codemagic.yaml `branch_patterns: main`）

### tsc 校验
`node_modules/.bin/tsc -b --noEmit` exit 0

### 长期约束更新
- 「3 年级起去重 1-2 年级已学词」机制已废弃，**以后别再引入类似的「跨年级硬过滤」逻辑**，
  除非基线来自不同教材（那才是真正的去重场景）
- 一线课标不再追加（课程标准已经在 SL/新版教材里）
- 三起线依旧是禁动区

## 设置页副标题确认：源码正确，浏览器/多实例缓存导致旧文案

- 02:07 用户截图显示设置页仍显示「1-9 年级，三年级起自动跳过已学词」
- 经排查：
  - `src/pages/SettingsPage.tsx` 已改成「按新起点 SL 真实教材 / 按外研新标准真实教材」
  - `git log` 与 GitHub 远端均确认 v23 commit `59be358` 已推送
  - `grep` 整个 `src/` 目录无「三年级起自动跳过」字样
- 原因：**本地同时存在多个 vite dev server 实例**（5173 与 5174），iPad 可能连到了 5173 的旧缓存实例
- 处理：用 `taskkill /F /IM node.exe` 结束所有 node，重新启动 `npx vite --port 5174 --host 127.0.0.1`
- 建议用户清浏览器缓存后用 `http://127.0.0.1:5174` 访问

## 祝贺页 UI 改版（预览图样式 + 按钮重排 + 二次确认）

### 用户原话
「把祝贺页改成这个预览页里的样式，但是有两点要修改：
1、继续学习按钮放在重新学习本单元下面；重新学习本单元按钮点击之后需要增加一个确认按钮。
2、把顶部的烟花图标换成目前正在使用的奖杯图标。」

### 改动文件
- `src/pages/LearnPage.tsx`（祝贺页 JSX 整体重写）
- `src/index.css`（新增 `breathe` 动画）
- `codemagic.yaml`（APP_BUILD 72）

### 样式改版（参考 `.workbuddy/preview/celebration-mockup.html`）
- 背景：浅紫渐变底 + 顶部柔光 + 白色渐变底部
- 奖杯：保留正式版 `TrophyIcon`，外加紫色 radial 柔光呼吸底盘
- 单元完成台词卡：金调渐变 `linear-gradient(135deg, #FFFAEB 0%, #FFF4D6 100%)` + 左侧金色竖条 + 装饰引号
- 年级完成奖励卡：白卡圆角 + 本轮星星/太阳 + 分隔线 + 累计太阳/星星
- 年级统计网格：6 格 2 列

### 按钮顺序调整
| 页面 | 顺序（上 → 下）|
|---|---|
| 单元完成 | 重新学习本单元 → 继续学习 → 返回首页 |
| 年级完成 | 继续学习 → 返回首页 |

### 二次确认
- 点击「重新学习本单元」后，原操作区替换为确认卡片：
  - 文案：「确定要重新学习本单元吗？当前单元的学习进度将重置，已得积分保留。」
  - 主按钮：确认重新学习
  - 次按钮：取消

### 提交与构建
- tsc -b --noEmit exit 0
- commit `13f59c9`
- push origin main：`59be358..13f59c9`
- APP_BUILD 71 → 72
- 因 codemagic.yaml 已按用户偏好禁用手动构建触发，构建需到 Codemagic 网页点「Start new build」

## 奖杯 PNG 透明瑕疵修复

### 问题
用户反馈祝贺页奖杯图像有缺失（杯身、杯口、底座处出现浅色斑块）。

### 根因
- 不是动画造成（`breathe` 只是外层容器整体缩放）
- 是 `src/assets/trophy-celebration-transparent.png` 本身有透明/半透抠图残留
- 新版浅紫/白渐变背景让这些透明区域显形，看起来像缺失

### 临时修复
`src/pages/LearnPage.tsx`：奖杯外层容器从紫色透明柔光改为**暖金色不透明 radial-gradient 底盘**
- 透明瑕疵被金色填充，视觉上恢复完整
- 奖杯本身加 `drop-shadow`，增强立体感

### 根本修复
仍需替换为无瑕疵的奖杯 PNG（或改回 SVG 自绘）。当前修复只是掩盖。

### git
- commit `56ace22`
- push origin main：`13f59c9..56ace22`
- APP_BUILD 仍为 72

---

## 祝贺页奖杯图替换为用户提供的新卡通金色奖杯（推送完成）

### 用户原话
「这个是背景透明的PNG图像，请用这个，把水印去掉」

### 做了什么
- 用 `C:/Users/huawei/.workbuddy/clipboard-images/clipboard-2026-09-09T12-49-49-396Z-d0dd140c.jpg`
  替换 `src/assets/trophy-celebration-transparent.png`
- 处理流程：
  1. 边缘 flood-fill 去除白色背景（保留奖杯主体与内部高光）
  2. 右下角 `1580,1810` 矩形区域直接裁透明，去除「豆包AI生成」水印
  3. 从 1920×1920 缩放至 1024×1024，控制包体积（2.86MB → 1.75MB）
  4. 保存为 PNG
- `src/pages/LearnPage.tsx`：奖杯外层容器从暖金色不透明底盘改回**紫色呼吸柔光底盘**
  （新图本身完整，不再需要金色底盘掩盖瑕疵）
- `src/components/TrophyIcon.tsx`：注释更新为新图源与处理方式

### git
- commit `fb39971`
- push origin main：`56ace22..fb39971`
- APP_BUILD 72 → **73**

---

## 祝贺页奖杯图重新抠图去网格（推送完成）

### 用户原话
「不行啊，背景为什么有网格」

### 根因
- 原 JPG 背景是白色且带 JPEG 压缩噪点
- 第一次处理用了「渐变透明」（240–255 线性 alpha），导致背景像素半透残留
- 浅紫渐变背景下这些半透明像素呈网格/斑点状

### 修正流程
1. 采样四角 40×40 平均颜色作为背景色参考
2. 颜色距离分割（距离背景色 < 26 视为背景）
3. 保留所有面积 > 500 的前景连通区域（奖杯、两侧麦穗、飘散彩带、星星）
4. 形态学闭运算（MaxFilter 5 → MinFilter 5）填充奖杯内部小孔洞
5. 轻微高斯模糊柔化边缘
6. 右下角 `1580,1810` 矩形强制透明去水印
7. 缩放 1920×1920 → 1024×1024

### 结果
- 浅紫/深紫背景预览均无网格
- 奖杯主体、麦穗、彩带完整保留
- 文件大小约 741KB

### git
- commit `1866aea`
- push origin main：`fb39971..1866aea`
- APP_BUILD 73 → **74**

---

## 重点记忆：删除「词条需学习一次才能移除」限制（推送完成）

### 用户原话
「删除"重点记忆列表的词条需要最少学习一次才能移除"的限制」

### 改了什么
- 删除 `Progress` 字段 `difficultStudiedIds`（及其在 sanitizeProgress /
  toGradeModel / freshProgress 中的处理）
- 删除 `LearnPage.tsx` 拼对一次即写入 `difficultStudiedIds` 的逻辑
- 删除 `DifficultWordsPage.tsx` 中：
  - `studiedSet` useMemo
  - `renderRemoveButton` 的 `studied` 判断分支（学习前灰按钮+禁用）
  - 页面底部「重点记忆词条至少学习一次才能移除」提示卡片
- 现在所有重点记忆词条都直接可移除（点「移除」→「确定」两步确认）

### git
- commit `24a6700`
- push origin main：`1866aea..24a6700`
- APP_BUILD 74 → **75**

---

## 排查音频 manifest 错位（推送完成）

### 用户原话
「音频和文本发现错位，这是人教版三年级起点，音频的point，提示是name。
请排查全部六条线的音频、文本、提示是否还有其他词条有这种情况」

### 排查方法
写 audit 脚本：扫描 6 条线全部 15488 条 entry，
对每个 `entry.english` 检查：
- `manifest.get(text.toLowerCase())` → id
- `idToText.get(id)` → 实际绑定文本
- 比较两个是否一致

### 发现 9 处错位
US manifest 2 处：
- `ox-g1u1e0003` 同时绑定了 `hello!` 和 `goodbye` → 应是 hello!（oxford G1U1 第 3 个 entry）
- `ox-g1u1e0008` 同时绑定了 `how are you?` 和 `how` → 应是 how are you?（第 8 个 entry）

UK manifest 另 7 处（多为英文标点变体串了 id）：
- `wy-g7u2e1278`: papercutting / paper-cutting
- `wy-g8u3e1896`: d / 3-d
- `wy-g9u4e2439`: name after / name... after
- `wy-g9u4e2442`: leave behind / leave... behind
- `wy-g9u4e2456`: copilot / co-pilot
- `wy-g9u6e2510`: longlasting / long-lasting
- `wy-g9u6e2520`: mistake for / mistake... for...

### 修复
每个错位 id 都「保留先入者、删后续重复 key」。被删的文本在运行时由
speechSynthesis 兜底朗读（不破坏学习路径）。

### 验证
audit 重跑 0 mismatches。

### git
- commit `eac36b4`
- push origin main：`24a6700..eac36b4`
- APP_BUILD 75 → **76**

---

## 音频/文本错位 BUG 二次深查（用户问"是否 100% 排除"）

上一轮只查了 manifest 内部自洽性，本轮补齐整条链路。结论：**结构性错位 100% 排除，
仅剩 mp3 人声内容无法程序化验证（需 ASR/人工听审）**。

### 五条独立证据
1. **manifest 往返一致**：15488 entry，text→id→反向 text，0 mismatch
2. **播放链路精确匹配**：`resolveAudio()` = `manifest.get(text.trim().toLowerCase())`，
   **无模糊匹配兜底**；未命中返回 null → speechSynthesis 读当前 text。
   所以不存在"查不到就播别的词"。
3. **生成脚本无索引偏移**：`scripts/generate_audio_edge.py:127-134`
   `key, text, tid = t["key"], t["text"], t["id"]` + `out = AUDIO / f"{tid}.mp3"`，
   text 与文件名来自**同一 task 对象**，不可能 off-by-one
4. **US/UK 双次独立生成时长交叉检查 0 差异** → 排除单文件随机录错
   （两次独立 TTS 不可能错成同一个别的词还时长相同）
5. **时长异常率仅 36/5331 = 0.67%，偏长仅 2 条** → 排除系统性整体串位
   （若整体偏移，异常率应接近 100%）

### 数据字段层面（用户截图 point 配 /neɪm/ 属此类）
- 粗扫：同一 english 音标/释义不一致 2104 组 → 全是正常的（多义词 dress 名/动、
  英美音标写法差异），噪音太大
- 消噪版（只取第一个 /.../ 段 + 归一化 ɑːr/ɜːr/ɔːr/ər → 去 r）：
  **29 条，全部是英美音标差异（ɒ/ɔ/ɑ、弱读 ə/æ、oʊ/əʊ），0 条真串位**

### 残留小瑕疵（非错位，未修）
- `skier`(wy-g5u7e1003) 3.02s、`swing`(ra-g8u23e1753) 2.66s —— 音频偏长，
  US/UK 一致，判定为 TTS 特性（拖长音/静音），不是错位
- `am` / `it` / `mm` 三条：english 是缩写，音标念全称（am→/ˌeɪ ˈem/、it→/ˌaɪ ˈtiː/、
  mm→/mɪlɪmiːtər/）。轻微不一致，音频按缩写生成

### 用户截图那条存疑（待用户确认）
- **renjiao3 G3U1「Making friends」里没有 point 这个词**（curriculum.ts:533，
  首条就是 name /neɪm/）
- `point` 在 **waiyanshe3 G3U1「Welcome to school」**（waiyanshe.ts:480，/pɔɪnt/ v. 指）
- ⇒ 截图标题"人教版三年级起点"却出现 point，需要用户确认是否选错教材线

### 新增排查工具（保留在 scripts/，未提交）
- `audit_audio_duration.py`：MP3 时长 vs 文本长度离群检测 + US/UK 交叉校验
- `audit_phonetic_outlier2.py`：同一 english 的音标少数派检测（已消噪）

---

## 🔴 音频与文本错位 BUG（严重，已修复，APP_BUILD 76 → 77）

### 用户报告
「确定就是人教版三年级起点，TestFlight 上也是一样，发音错误」——
显示 `name` 却听到 `point`；要求排查根因并重新核查美音英音共 12 组全部对应情况。

### 结论
- **属实**。铁证：`wy-g1u2e0020.mp3`（manifest 挂给 name）与有道 "point"
  PCM 互相关 **0.9994**，与有道 "name" 仅 0.2749。
- **根因**：音频以 entry.id 命名，id 随词库重建整体位移；多个补齐脚本
  各自按下标配对 text 与 id ⇒ 结构性错位，必然复发。
  （此前"已 100% 排除错位"的结论方法有误：只验了 text→id→text 往返自洽，
   没验 mp3 文件内容。）
- **真实错位率**（修正判据后抽样）：美音 200 条中 18 条（9.0%）无法与正确基准
  对齐，其中 13 条（6.5%）明显念错；英音 150 条中 22 条（14.7%）。

### 修复
1. 文件名改为 `t + blake2b(文本)` 哈希，与 entry id 解耦（App 逻辑不用改，
   manifest 的 value 直接当文件名）。
2. 全量重生成 4865 唯一文本 × 2 口音 = 9730 文件（有道优先 / Edge 兜底）。
3. 删旧命名文件 15829 个，public/audio 412M → 194M。
4. **12 组（6 线 × 美/英）覆盖率 100%，缺失 0**；新库抽查 60/60 全部正确。

### 两个让自己误判的坑（写进 topics/audio-pipeline.md 了）
1. 单音源比对：跨音源同词相关度只有 0.2–0.32 ⇒ 虚报成 58% 错位。
2. 用小写 key 合成：有道对 `ben`/`Ben` 返回不同录音 ⇒ 虚报成 35%。

### 改动文件
- `public/audio/` 全量换血（9730 新增 + 15829 删除）
- `public/audio/manifest.json`、`manifest-uk.json`（4865 条，值改为哈希名）
- `src/lib/audio.ts`（只加注释说明 manifest value 已非 entry id）
- `codemagic.yaml` `APP_BUILD: 76 → 77`
- `scripts/` 新增 5 个排查/生成/清理工具
- `.workbuddy/memory/topics/audio-pipeline.md` 新增事故章节
