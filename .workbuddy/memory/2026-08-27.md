# 2026-08-27 安妮英语听写开发日志

## 选择单元 Tab 简化为两个下拉框
- 用户需求（2026-08-27 00:03）："选择单元"标签页过于繁琐，改为两个下拉框（年级/单元）+ 开始学习按钮。
- 实现（`src/pages/HomePage.tsx`）：
  - 移除横向年级 chip 列表和纵向单元列表。
  - 改为两个 `<select>` 下拉框："选择年级"、"选择单元"。
  - 切换年级时，单元下拉框自动重置为该校年级的第一个单元。
  - 默认选中当前年级的第一个单元，按钮直接可用。
  - 下拉框右侧加 SVG 下拉箭头（`appearance-none` 隐藏原生箭头后自定义）。
- 验证：tsc 通过；Edge 无头截图 `?preview=unit`（临时 DEV 入口，截图后移除）确认 UI。
- 当前状态：已与 codemagic.yaml（APP_BUILD 34→35）一并 commit 并 push，触发 Codemagic 构建 35。无声 BUG 真机未再复现，本轮不排查、直接上真机验证。

## 学习页音频缓存排查（重点记忆列表学习久了卡死崩溃）
- 用户现象：重点记忆列表学习页"学习久了会卡顿，甚至 APP 直接卡死崩溃"。问音频缓存是否有清理。
- 结论：**音频缓存有清理机制，但不是导致崩溃的主因；崩溃来自"已解码音频缓冲长期常驻"逼近 iOS 内存上限。**
- 已核实的代码事实：
  - `src/lib/audio.ts`：`audioCache` 是模块级 Map，硬上限 `MAX_CACHE_ENTRIES = 48`，FIFO 淘汰，淘汰时 `releaseElement()`（`pause + 清 src + load()`）主动释放原生解码资源；`useSpeechLoop` 有 generation 守卫、卸载 `stop()`、前后台切换清理、播放失败 retry 时 `releaseElement` 旧元素。定时器 `src/lib/timer.ts` 正常（完成即删，无堆积）。
  - `difficultOrder` 在 `App.startDifficultLearning` 中一次性 `shuffle` 后 `setState`，稳定；遍历完 `goNext` 置 `difficultDone` 弹完成页，**不无限循环**。
  - 关键设计缺陷：`ensureElement` 把 `HTMLAudioElement` 回写进 `AudioResult.el` 并**长期驻留 cache**。一旦 `preload=auto` 或播过，WebKit 解码缓冲常驻内存，直到被 FIFO 淘汰才 `releaseElement`。
    - 列表 < 48 词条时，size 永远 < 48，**淘汰从不触发，所有 el 永久常驻且全解码**（反直觉：小列表反而更占内存）；
    - 列表 > 48 时 steady-state 也常驻 48 份解码缓冲。
  - 重点记忆列表通常更长、含更多长难词/短语/句子（解码后单条约 1-3MB），且用户常一口气学完长列表（无单元完成页打断节奏），48 份常驻解码缓冲在低端 iOS 上可达 100MB+，触发系统 Jetsam 杀进程 → "卡死崩溃"。普通年级学习按单元（10-20 词）推进、每单元完成页打断，不易触顶。
- 修复方向（待用户确认）：
  - 方案 A（治本）：`audioCache` 只缓存 `{url, source}`（不含 el）；`HTMLAudioElement` 改为运行时按需创建、播放完/切题/淘汰时 `releaseElement` 释放，不长期驻留。cache 近零内存，el 用完即放。需改 `audio.ts` + `useSpeechLoop.playViaElement`（去掉对 `item.el` 的常驻依赖）。
  - 方案 B（快速止血，低风险）：`MAX_CACHE_ENTRIES` 48→16、预取深度 3→1~2、淘汰先 `delete` 引用再 `releaseElement`；保留预取元素但上限收紧。可先推一版真机验证。
- 状态：已实施方案 A（根治）。改动：`audio.ts` 的 `audioCache` 只缓存 `{url,source}`（移除 `el` 字段）、新增 `createAudioElement(url)` 工厂、`prefetchAudio` 改为临时 `<audio>` 元素预热（canplaythrough/error 后 `releaseElement`、2s 兜底释放）；`useSpeechLoop` 的 `playViaElement` 每题 `createAudioElement` 新建元素并在开头 `releaseElement(audioRef.current)` 释放上一题、`stopAll` 用 `releaseElement`、`visibilitychange` 回前台去掉 `item.el=undefined`。`tsc -p tsconfig.app.json` 与 `vite build` 均通过。已升 `APP_BUILD 35→36` 并 commit+push，待真机验证重点记忆长列表学习内存是否稳住（不再卡死崩溃）。

## 学习首页（HomePage）点击头像进入用户资料页
- 用户需求（2026-08-27 20:06）：学习首页增加"点击头像进入用户资料页"。
- 现状：`ProfilePage` 已存在；`SettingsPage` 已有"点击头像进资料页"范式（`onOpenProfile` → `setView("profile")`）。但 HomePage 头像此前只是展示、无点击入口，App 也未给 HomePage 传 `onOpenProfile`。
- 改动：
  - `src/pages/HomePage.tsx`：props 增 `onOpenProfile?`；顶部头像 `div` 改为 `<button>`（`onClick={onOpenProfile}`、`title/aria-label="用户资料"`、`cursor-pointer`），与 SettingsPage 风格一致。
  - `src/App.tsx`：新增 `profileFrom` 状态（`"settings" | "home"`，默认 `"settings"`）。SettingsPage 的 `onOpenProfile` 置 `profileFrom="settings"`；HomePage 新增 `onOpenProfile` 置 `profileFrom="home"`；`ProfilePage` 的 `onBack` 与左滑返回 case `"profile"` 均改用 `setView(profileFrom)`，使"从首页进则回首页、从设置进则回设置"。左滑返回 effect 依赖数组补 `profileFrom`。
- 验证：`tsc -p tsconfig.app.json` 通过（exit 0）；dev server 热更新。未推送（用户未要求升构建号）。
- 注意：返回层级需区分来源，不能写死回 settings，否则从首页点头像进 profile 返回会跳到设置页，体验断裂。

## 后台长时间唤醒后键盘非英文小写（需手动切换）
- 用户现象（2026-08-27 22:16）：放后台时间比较长后唤醒，键盘不是默认英文小写全键盘，需手动切回。
- 根因：iOS 在 app 从后台恢复时，对已聚焦的 input **不会重新评估 inputmode**，会直接沿用后台前的中文输入法。原 `SpellingInput` 的 `onVis`（visibilitychange）在「答题中」分支只做了 `flushSync(setInputKey+1)` 重建 input DOM，没有先让旧 input 失焦，于是 iOS 直接把后台前的中文键盘带回来。
- 修复（`src/components/SpellingInput.tsx`）：在 `onVis` 进入重建前，统一先 `inputRef.current?.blur()`（对答题中/完成态都先失焦）；答题中分支再 `flushSync` 重建 input，新 input 经 ref callback + useLayoutEffect 重新应用 `inputmode="latin"`，迫使 iOS 在 resume 时重新读取并切回英文小写。
- 验证：tsc 通过；issue 为 iOS 平台行为，需真机（TestFlight）验证。未推送（用户尚未要求升构建号）。
- 补充：`inputmode="latin"` 是 iOS WebKit 私有扩展值，冷启动首次进入靠 ref callback + useLayoutEffect 生效；后台恢复靠 blur→rebuild→focus 强制重新评估。若真机仍残留中文，下一步可尝试 focus 时加 rAF 延迟或调整 inputmode 组合。

## 设置页「关于」开发署名变更
- 用户需求（2026-08-27 22:2x）：设置页「关于」弹窗中「开发：Luminous Soul Youth」改为「开发：NO3NO4」。
- 改动：`src/pages/SettingsPage.tsx` 第 273 行。未推送（连同键盘修复一并 push）。

## 正式发布（App Store 上架）准备（用户要求"全部"执行）
- 用户询问正式发布需要做什么，逐条落地以下 4 项：
  1. **隐私清单**：在 `codemagic.yaml` 新增步骤「生成并登记 PrivacyInfo.xcprivacy」——写入 `ios/App/App/PrivacyInfo.xcprivacy`（声明 UserDefaults=CA92.1、FileManager=C617.1，无数据收集、无跟踪），并幂等注入 App 工程 Resources 阶段；脚本 try/except 失败不阻断构建（需在 Codemagic 测试构建中验证清单确实进 IPA）。
  2. **自动提审配置**：`publishing.app_store_connect` 增加 `submit_to_app_store: false`（默认关闭，附注释说明：ASC 后台 metadata 填齐后再改为 true 即可 CI 自动「提交审核」）。
  3. **隐私政策页面**：新建仓库根 `privacy-policy.html`（中文，声明全部学习数据仅本地存储、不收集/不共享/无第三方 SDK、不请求麦克风等权限、儿童隐私与联系方式占位邮箱）。需挂到静态托管拿 URL，建议 GitHub Pages（仓库 Settings→Pages→main 分支→/root，得 `https://linshaoyun11.github.io/annie-english-dictation/privacy-policy.html`），或 CloudStudio 一键部署。
  4. **App Store 上架文案**：名称/副标题/描述/关键词/截图清单/类别(教育)/年龄评级(4+)/隐私申报(无数据收集) 已在回复中起草。
- 注意：上述均为 release-prep 改动，本次未提交（用户仅说"全部"执行动作，未明确"推送"）。提交前需先在 Codemagic 跑一次构建验证 PrivacyInfo 步骤注入成功。

## 隐私政策 URL 不可访问排查与修复
- 用户反馈 `https://linshaoyun11.github.io/annie-english-dictation/privacy-policy.html` 404。
- 原因：① `privacy-policy.html` 尚未 commit/push（仓库里没有该文件）；② GitHub Pages 未开启（github.io 根路径 404，仓库本身 200 存在）；本机无 gh CLI，无法代开 Pages。
- 处理：将 `privacy-policy.html` + codemagic.yaml（含 PrivacyInfo 步骤、submit_to_app_store: false、APP_BUILD 38→39）+ 记忆日志一并 commit/push（顺带借 build 39 验证 PrivacyInfo 注入）；另部署 CloudStudio 静态页拿立即可用的隐私政策 URL。GitHub Pages 需用户在仓库 Settings→Pages 手动开启（选 main / root）。

## 隐私政策联系邮箱更新 + 正式发布指引
- 用户真实邮箱 `linshaoyun11@163.com`（用户原话写成 163.om，经确认实为 163.com）。已替换 `privacy-policy.html` 占位邮箱（mailto + 显示文本），CloudStudio 部署页已重新部署并 curl 验证包含真实邮箱，URL 不变：`https://169a57c265b0409ea5f26702f1433b81.app.workbuddy.link`。
- 邮箱改动随 `APP_BUILD 39→40` 一并 commit/push（避免与 build 39 重复构建号导致 TestFlight 上传失败）。
- 发布操作要点（已告知用户）：等 build 40 → TestFlight 终验（键盘唤醒、重点记忆内存、单元练习）→ ASC 网页端填元数据（名称/副标题/描述/关键词/截图 6.9"+6.5"/隐私政策 URL/App Privacy 不收集/年龄评级 4+/类别教育/技术支持 URL）→ 版本页选 build 40 → 提交审核 → 1-2 天过审 → 手动/自动发布。`submit_to_app_store` 开关待 metadata 齐后再置 true。

## 技术支持页（ASC 必填的「技术支持 URL」）
- 新建仓库根 `support.html`：App 简介 + 常见问题（数据本地存储、无声排查、键盘切英文、重点记忆/单元练习区别、无账号原因）+ 联系邮箱 linshaoyun11@163.com + 与隐私政策互链（页脚互挂，署名 NO3NO4）。
- CloudStudio 同站部署已更新（curl 验证 200）：隐私政策 `https://169a57c265b0409ea5f26702f1433b81.app.workbuddy.link`（index.html）、技术支持 `.../support.html`。
- 随 `APP_BUILD 40→41` commit+push。ASC 填写：隐私政策 URL 用主链接、技术支持 URL 用 `/support.html`。

## 后台唤醒偶发问题加固（播放失败 / 键盘非英文小写，build 41 真机仍偶发）
- 用户反馈：偶尔后台切回来播放失败；偶尔还是没有默认键盘（中文）。
- 根因判断：两处恢复动作都在 `visibilitychange` 一触发就立即执行，而 iOS 此刻仍在恢复过渡期（WKWebView/AVAudioSession 未就绪）→ play() 落在坏窗口失败；新 input 被绑到后台前旧键盘会话（中文）。
- 加固（`src/hooks/useSpeechLoop.ts`）：
  - visible(audio) 分支：`playCurrent` 延迟 **350ms**（`resumeTimerRef`，hidden/stop 时清除，延迟后重新校验有效性）；
  - `playViaElement` 重试升级：`attemptNo` 0/1/2，失败换新元素按 **80ms→600ms** 递增延迟重试两轮后才算条目失败；
  - 看门狗增加停滞检测：到预期结束时刻仍未 ended 且 `currentTime < dur-0.3`（state=playing 但进度不走）→ 强制重播（覆盖回前台静默僵尸态）；
  - visible(speech) 分支加 `speechSynthesis.pause()+resume()` 解卡 hack（iOS 经典 BUG：恢复后 speaking=true 但无声）。
- 加固（`src/components/SpellingInput.tsx`）：
  - onVis 答题中分支：blur 后**延迟 200ms** 再 `flushSync` 重建 input（`resumeRebuildTimerRef`，effect cleanup/重进时清除，延迟后再校验 visible），重建后**双 rAF 补一次 focus**——等 iOS 恢复过渡完成再重建，避免新 input 绑旧中文键盘会话。
  - 已知代价：极端情况下唤醒后键盘可能收起，需点一下屏幕（onDocClick 会带手势 focus）重新弹出，弹出的即英文小写。
- 验证：tsc exit 0；dev 热更新。未推送（用户未要求升构建号）。
