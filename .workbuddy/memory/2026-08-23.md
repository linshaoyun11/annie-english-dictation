# 2026-08-23 工作日志

### 排查人教版/沪教牛津是否存在起点混合与词条重复问题（用户 01:10 提问）

分析脚本：`scripts/analyze_dup_all.cjs`、`scripts/analyze_rj_cross.cjs`（用 esbuild 打包 curriculum.ts 后跑真实数据）。

**人教版（结论：存在与外研社相同的起点混合问题）**
- 数据来源：1-2 年级取自《新起点》（一年级起点），3-6 年级取自 PEP（三年级起点），7-9 Go for it（curriculum.ts 头部注释明确写明）。
- 人教社确实发行两种小学教材线：新起点（一起）与 PEP（三起），两条线都从零起点内容开始。
- 跨线重复：1-2 年级已学词在 3-9 年级重现 148 词次，分布在 44 个单元；最重的是三年级 U6（10/10 全重复）、U2（8/10）、4U9（8/10）。
- 若按一年级起点跨线去重：会清空 1 个单元（3U6），产生 2 个薄单元（3U2、4U9 各剩 2 词）。
- 3-9 年级内部自身复现 32 词次（PEP 教材正常复习性复现）。

**沪教牛津（结论：无起点问题，重复可忽略）**
- 单一教材线：牛津上海版一年级起始（1A~6B）+ 初中牛津上海版（7A~9B），无两条起点线混用。
- 跨单元重复仅 62 词次，且几乎全在 7-9 年级（初中复现小学词汇，属教材正常的复习性复现）；3-6 年级仅 4 词次重复。
- 两种教材单元内均无重复，词条 id 均唯一。

待用户决定是否给人教版做与外研社相同的「起点选择」处理（waiyanshe/waiyanshe3 模式可直接复用）。

### 实现人教版起点选择（用户确认后，APP_BUILD 升 15）

用户选择「同样加起点选择」。改动：
- `curriculum.ts`：新增 `renjiao3`（三年级起点，仅 3-9 年级，PEP+Go for it 原样不去重，161 单元/1955 词条）；`renjiao` 语义改为一年级起点去重线（1-9 年级，3 年级起过滤 1-2 年级已学词，移除被清空的三上 U6，184 单元/2047 词条，跨线重复 0）。派生方式与外研社完全同构（RENJIAO_G1_START/RENJIAO_G3_START）。
- `progress.ts`：迁移逻辑泛化为 G1_LINE_TAGS 映射（renjiao→"rj-g1"、waiyanshe→"wy-g1"），旧混合线人教进度自动迁移（保留学习记录，重置位置指针）；freshProgress 相应设置 lineTag。
- `SettingsPage.tsx`：教材列表变 5 项，renjiao 拆两条线。
- 未升 CURRICULUM_VERSION（沿用 lineTag 方案，避免进度整体重置）。
- 验证：tsc 通过；esbuild 打包实测 g1 线跨线重复 0、g3 线保留 148 处重叠、3U6 在 g1 线移除/g3 线保留 10 词、id 均唯一。
- 沪教牛津确认单一教材线，无需处理。
