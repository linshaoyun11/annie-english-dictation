# 2026-09-14

## 六线词条补词性 + 数据修复（build 109）

- 用户报仍有单词缺词性（截图 clean）。扫描：六线 word 词条 15979，build 90 那轮
  只补了 SL 派生线的部分；本轮缺口 1100 条（真·无任何词性标记，允许开头 (=...) 括注）。
- 三轮填充（脚本 .workbuddy/tmp/fill_pos.py / fill_pos2.py / verify_pos.py）：
  1. 库内参照（六线已标注 + kebiaoBank 1619）：同词/屈折基形（-s/-ed/-ing/-er/ies→y）
     参照，按义项字符重合度逐一配词性，多词性按参照顺序重组 → 677 条。
  2. 人工标注表（EN_TAG 单词性 + REWRITE 多词性）：411+8+3 条——短语型词条
     （look up v. / right away adv. / no matter conj. / would rather modal v. 等）、
     过去式条目、序数词、感叹词、专有名词、复合名词、国籍词。
  3. ECDICT stardict.db（851MB sqlite，release 下载）交叉校验 3389 词：
     pos 单字母码表 n/v/j(adj)/r(adv)/m(num)/d(det)/p(pron)/c(conj)/u(int)/i(prep)/a(art)；
     归一后残差 83 条全部人工判读，抓出真错配 30+ 处（closed/wounded/pleased/
     celebrated/detailed/spelling/warm×5/Chinese/front/paper/key/close/video/fair/
     following/cross/clean 二义组混装/English/Chinese/orange/west/human/total/welcome/
     separate/middle/prefer/metal 等）——多为「义项全是…的却标 v./n.」或
     「adj 义项混进 v. 组」。精准 sed 修复（每条先 grep 验唯一性）。
- 数据修复：unitl→until、satisty→satisfy（拼写错误，音频 manifest 里 until/satisfy
  已有，安全）；sketchbook 生写薄→写生簿；sportsperson 释义错挂成 Asian 的→运动员；
  artistic 释义为空→补；told 缺左括号；蹺蹺板→跷跷板。
- 终态：15979 word 词条缺词性 = 0；tsc 过；APP_BUILD 108→109。
- 工具留存：scan_missing_pos/coverage_pos/fill_pos/fill_pos2/verify_pos/
  crosscheck_ecdict/scan_adj_mismatch（.workbuddy/tmp/）。
- 注意：curriculum.ts 里也有词条定义（改了 226 行）；movieQuotes 句子线无词性概念不处理。
