# 2026-09-15

## 用户资料页改版（仅出预览图，未改代码）
- 用户诉求：① 重设计「修改密码」的 🔑 与「已学 334 词」的 📖 两个 emoji 图标；
  ② 页面整体重做、更规范。**明确要求先发预览图确认，不要直接改代码。**
- 现状实现：`src/pages/ProfilePage.tsx` —— 头部 nav（返回 36px 圆钮 + h1 18/600）→ 身份卡
  （头像 64 + 名字 + 加入日期 + `⭐ 1947 积分 / 📖 已学 334 词` 单行小字）→ 单个
  `rounded-2xl` 列表项（🔑 在 `h-9 w-9 rounded-xl bg-primary-lighter` 底衬里）。
  ⚠️ 该文件同时承载「内嵌三步改密码流程」（old→new→confirm + input 僵尸态重建
  `inputKey` / `inputAliveRef` + 全局 keydown 兜底）——改版**只动展示层，别碰这套逻辑**。
- 交付：`.workbuddy/preview/profile-redesign-v1.html` + 同名 `.png`（1960×3138，2x）。
  内容＝现状/新版双机对比 + 图标细节（36 底衬 / 56 放大 / 改前 emoji）+ 备选方案 + 改动点。
- 新版设计要点：emoji 全换 SVG（24 viewBox / 1.75 描边 / currentColor，与全站线性图标同规格）；
  积分与已学词从单行小字升为**双列数据条**（图标 + 17px 数字 + 11px 标签 + 1px 分隔线）；
  身份卡内用 1px 细线分区；设置项收进「账号安全」分组卡（行高 60、底衬 36、圆角 20）；
  星星沿用现有品牌 StarIcon（🎁 已有，不重画）。备选：挂锁 / 合起的书。
- **待用户确认后**才动 ProfilePage.tsx；须新增图标组件（建议 `src/components/` 下
  建 `ProfileIcons.tsx` 或并入现有图标文件），并升 APP_BUILD。

## 用户资料页改版落地（build 113，按用户确认的「方案 1」）
- 新增 `src/components/Icons.tsx`：`KeyIcon` / `BookIcon`。统一规格 24 视口 / 1.75 描边 /
  圆头圆角 / `currentColor`（颜色由 `className` 给，如 `text-primary`）。
  钥匙＝环柄(r4.5 cx7.4)+直齿杆+2 齿，环内 `fillOpacity .18`；书本＝翻页书左右两页
  `fillOpacity .14`，**填充路径加 `stroke="none"`、轮廓单独用不闭合 path 画、
  中缝再单独一条** —— 否则同一条边被填色路径的 z 边 + 轮廓 + 中缝叠描 3 次，小尺寸发糊。
- `src/pages/ProfilePage.tsx` 展示层重排：身份卡（头像 68px + `boxShadow` 浅紫环
  `0 0 0 3px var(--color-primary-lighter)` 取代原来在白色卡片上**肉眼不可见的 `ring-white`**，
  字号 19px）→ 1px `bg-border-light` 分隔 → 双列数据条（StarIcon 16 / BookIcon 17 +
  17px `tabular-nums` 数字 + 11px 标签，列间 `border-l`）→「账号安全」分组标题
  （12px/600 `tracking-[0.04em]`）→ 分组卡 `rounded-[20px]` 内单行 60px
  （底衬 36px `rounded-[11px]` + KeyIcon 20 + 15px 文字 + chevron，`active:bg-primary-lighter`）。
  改密码三步流程卡片只加了 `shadow-card`，**逻辑（input 僵尸态重建 / 全局 keydown 兜底 /
  flushSync）一行未动**。
- 验证：`.workbuddy/tmp/profile-real-390.png`（列表态）、`profile-real-390-step.png`
  （流程态）、`profile-zoom-row.png` / `profile-zoom-stats.png`（6x/5x 放大看小图标）。
  探针页 `probe-profile.html` + `src/probe/profileProbe.tsx`（mock 猴子/1947/334，
  `?step=old` 自动进流程），**untracked，不进构建**（vite 只以根 index.html 为入口）。
- ⚠️ 本次踩坑：`git check-ignore` 确认 `.workbuddy/preview/` **整个目录被 gitignore**，
  改版预览图不会进仓库（只作本地交付物）——所以预览 HTML/png 不必也不能提交。

