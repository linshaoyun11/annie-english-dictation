# 2026-09-11

## 提示面板显示英文答案 BUG 修复（build 81）

### 现象与根因
- 外研一线（waiyansheSL）学习页点「查看提示」直接显示英文单词本身（如 "ancient"），不是音标+译文。
- 根因是**数据问题**而非渲染逻辑：SL 两条线大量词条 `w(g,u,"ancient","","ancient")` —— phonetic 空、chinese 填了英文本身。
- 规模：waiyansheSL 1218 条、oxfordSL 1065 条（共 2283）；其余 5 条线 0 问题
  （oxford 3 条、renai 33 条 badPhonetic 是句子/短语，本来无音标，渲染跳过，正常）。

### 修复方式（自动回填 + 义项级合并）
- 脚本 `.workbuddy/tmp/fix-sl.mjs`：建来源词典（grades4to9/renjiaoSL/waiyanshe/oxford/renai
  + wySL/oxSL 自身好条目 + kebiaoBank + 手工兜底词典 manual-dict.json 289 词），
  按 english（小写 key）回填音标；释义**按词性分组、义项级去重合并**——满足
  「多义词显示所有含义」。2283 条全修复，0 缺失。
- 释义合并关键：复合词性释义（"v. 想知道 n. 奇观"）要按词性标记切成义项序列，
  义项再按 ；切分去重；丢弃纯标点碎片；输出词性标签去掉多余句号。
- manual-dict.json 是我手写的兜底（数字/星期/基础词/短语/专名，其他线没有这些低年级词）。

### 校验（全过）
- 重扫：badChinese 两线归零；tsc 通过；无引号转义问题。
- **g/u/english 三元组 diff 前后完全一致** → entry id 不变、音频 key（文本哈希）不变
  → 不需要重生成音频、不需要升 CURRICULUM_VERSION、用户进度无损。
- APP_BUILD 80→81。

### 踩坑（脚本层面）
- while 条件里 `new RegExp(...).exec(s)` 每次迭代新建正则 → lastIndex 归零 → 死循环
  （跑了 7 分钟才发现）。正则必须在循环外建一次。
- mk() 行的捕获组从 en 开始（m[1]=en m[2]=ph m[3]=cn），与 w()（m[1]=g m[2]=u，
  STR 从 m[3] 开始）不同，第一次就错在这里导致好线数据全没收上。

### 状态
- 已提交推送 main（build 81）。Codemagic 仅手动触发，需手动 Start new build。

## 核查：外研一线（waiyansheSL）初中段数据是合成词表，非真实教材

用户反馈「初一单元内容感觉和一年级差不多」。核查结论：**年级映射没错，但初中 6 册内容数据是合成的**。

### 年级映射（正确，勿改）
- 文件里 `grade` 是**册号** 3–18（2A–9B），`g = Math.ceil(册/2)`：
  册13+14 → G7（初一）、册15+16 → G8、册17+18 → G9。
- 所以 App 里「初中1年级」= 册13(7A) + 册14(7B)，映射无误。

### 初中段（册13–18）是合成数据的证据
1. **结构**：每册恰好 10 单元 × 每单元恰好 10 词；小学段是 20 单元 × 4–6 词。
2. **标题**：抽象主题（My family / My body / Travel / Sports and games）；
   小学段是具体课文名（"I visited my grandmother." / "I went to the cinema."）。
3. **词条**：主题分类词表（head/face/eye/ear…、plane/train/bus/ship/bike…），
   不是课后词表；册13 前 4 单元 = hello / 数字1–12 / 颜色 / 星期。
4. **难度倒退**：平均词长 册13=5.6 ≈ 册11(6上)=5.6 < 册7(4年级)=6.0。

### 附带发现
- 册1/册2（1A/1B 一年级上下）**数据不存在** ⇒ 外研一线 App 里没有一年级内容。
- 小学段（册3–12 = 2A–6B）为真实录入，可信任。

### 待办
初中 6 册需按真实教材重录，需用户提供 7A/7B/8A/8B/9A/9B 的
CONTENTS + Vocabulary 页截图（流程见 skill annie-rebuild-curriculum）。
**不要自行用第三方目录改数据**（用户既定规则：以用户截图为准绳）。

## 三条「一年级起点」线按官方教材第三次重录（v25，build 81+）

用户指令：无照片，需网上自行查证、尽量走官方渠道；**不许动**三起人教/外研、仁爱
（这三条是拍照真实录入）；只重录一年级起点三套；每套从一年级起逐年级排查。

### 数据来源链（可复用）
1. **单元结构/标题 —— dzkbw 电子课本网（教材原页扫描，等同官方）**
   - 外研一线 `http://www.dzkbw.com/books/waiyan/yingyu/yq{1-6}{s,x}/001.htm`
   - 沪教牛津 `http://www.dzkbw.com/books/hjb/yingyu/oxford{1-6}{s,x}/001.htm`
   - 人教新起点 `http://www.dzkbw.com/books/rjb/yingyu/xqds{1-6}{s,x}/001.htm`
   - 编码 gb2312；目录在 `<meta name="description">` 里，按 `|`/逗号切分最干净。
   - **⚠️ 该站只有小学册，初中册（yq7s/oxford7s…）一律 503，别浪费时间探测。**
2. **词条+音标+释义 —— yyld.51jiaoxi.com 分册单词表**
   - `https://yyld.51jiaoxi.com/{yishang|yixia|…|liuxia}/{code}_danci.html`
     code：人教新起点 `rjbxqd`、外研一起 `wybyq`、牛津上海版 `njshbdb`
   - 初中：`https://yyld.51jiaoxi.com/{qishang|qixia|bashang|baxia|jiushang|jiuxia}/{code}_danci.html`
     code：人教新目标 `rjxmbb`、外研 `wyb`、沪教牛津 `hjnjb`
   - 结构：`<li class="u-item…">单元名</li>` + `<li class="dc-item…">` 内
     `<p class="en">词</p><span>音标</span><span>翻译：中文</span>`
3. 缺口补充：kkabc（`https://fanyi.kkabc.com/list/bk_xxxx`）用于外研三下、牛津六上。

### 踩过的坑（下次直接避开）
- **Starter Unit 排序**：人教新目标初中含 Starter Unit 1-3，`unit_num("Starter Unit 1")`
  与 `unit_num("Unit 1")` 都是 (1,)，stable sort 会让两者交错成
  SU1,U1,SU2,U2… 排序键必须带 starter 标志位。
- **牛津 Unit 编号在 Module 内重复**（4 Module × Unit 1-3），按编号映射会把
  G1U1 错标成最后一个 Module 的 "On the farm"。**只在编号全局唯一时才按编号映射**，
  冲突即整册放弃映射（宁可留 "Unit 1" 空壳也不猜）。
- **官方目录偶有缺条**（外研六上丢了 Module 9 Unit 1），要求"总数相等才应用"
  会整册跳过 → 改成按 Module 号逐个匹配、缺则退回 Unit 2 / 只留 Module N。
- 生成脚本 `export` 名被旧硬编码 `WAIYANSHESL_PLACEHOLDER` 覆盖，tsc 报
  "has no exported member"。正确导出名 `WAIYANSHES_SL_CURRICULUM`。

### 结果（数据文件）
| 线 | 单元 | 词条 | 年级 | 备注 |
|---|---|---|---|---|
| renjiaoSL | 124 | 3839 | 1-9 | 小学新起点 + 初中新目标 |
| waiyansheSL | 175 | 2892 | 1-9 | 小学 Module 级，初中新旧版并存 |
| oxfordSL | 108 | 2501 | 1-9 | 沪教牛津；**无六年级下**（站点无数据） |

- 单元标题：外研/牛津/人教小学段已用 dzkbw 官方课文名补全；
  初中段（人教 G7-G9、外研 G7下/G8下/G9下、牛津 G1/G7）官方站无数据，
  **保留 "Unit N" / "Module N" 空壳，未编造**。
- grade 直接是真实年级 1-9，不再有「册号→年级」归并，
  `mergeBooksToGrades` / `dedupeEarlyGrades` 已删（真实教材有螺旋复现词，去重会误删）。
- CURRICULUM_VERSION 24 → 25（entry id 全变，老进度重置）。
- 音标用项目内 5 条线 + 课标库按词回填（5061 词词典），短语仍无音标属正常。
- 音频：新增 1199 个文本（×美英 = 2398 文件），用
  `scripts/regen_audio_by_text.py` 增量生成（需用 venv 的 python，否则 edge_tts 缺失）。

### 补记：音频文本清单千万别配错文件
`.workbuddy/tmp/build_texts.py`（生成 regen_audio_by_text.py 的输入）里
**一线与三起线是不同文件**：renjiao(一线)=`renjiaoSL.ts`，
renjiao3(三起)=`curriculum.ts`+`grades4to9.ts`。
第一次配错导致人教一线 390 个词（Asia / Berlin / 人名地名等）没进清单、没生成音频。
重跑前先抽查一个该线独有的词是否已在 texts-by-line.json 里。

## 祝贺页顶部白条/底部黑条修复（21:46–22:11，APP_BUILD 83）
- 根因：body 用 env(safe-area-inset-*) 做 padding，祝贺页覆盖层 `absolute inset-0`
  挂在安全区以内 ⇒ 状态栏/手势条区域露出 body 纯色 #F8F7FF，与祝贺页渐变
  （顶部紫光晕、底部纯白）在交界处形成色差 ⇒ "白横幅/黑横幅"。
- 修复：覆盖层 `absolute` → `fixed inset-0 mx-auto max-w-md`（fixed 相对视口，
  viewport-fit=cover 下铺满物理全屏），渐变延伸全屏；内容 padding 改
  `pt-[calc(3rem+env(safe-area-inset-top))]`；加 `overscroll-behavior: contain`
  防滚动链拖出 WKWebView 黑底。tsc 过；dev ?preview=grade 截图验证布局正常。
- ⚠️ 排查环境大坑（复用价值高）：无头 Edge 的 --user-data-dir 放项目内
  （.workbuddy/edgeprofile）会被 vite watcher 监听，Edge 持续写 Code Cache ⇒
  vite 无限 `page reload` 循环，页面永远白屏。profile 必须放项目外
  （如 C:/Users/huawei/.workbuddy/edgeprofile-shot）。另：两个 vite 实例
  同听 5180 也会导致 hash 不匹配反复整页刷新。截图脚本（ESM）须放在
  node workspace 里跑（import 解析跟脚本路径走）。

## 词库括号词条清理（22:43–22:50，APP_BUILD 84，commit 待查）
- 用户截图仁爱线看到 `a (an)`（实际词条在人教一线 G7U1）。全量扫描六线：
  114 条英文含括号（renjiaoSL 64 / renjiao3 13 / waiyansheSL 20 / waiyanshe3 1 /
  oxfordSL 16 / renai 0）。
- 处理：**删括号及内容、压空格、去标点前空格**（非整条删除，保住词条本体）：
  `a (an)`→`a`、`goose (pl. geese)`→`goose`、`keep(kept/kept)`→`keep`、
  `(be) unaware of`→`unaware of`、`would (d) like to`→`would like to`、
  `Congratulations (on ...)!`→`Congratulations!`、`photo (= photograph) (pl. photos)`→`photo`。
  同单元规范化后撞车 0 条 ⇒ 0 删行。**只做同单元去重，绝不做跨单元去重**（螺旋复现教训）。
- 词条 id 不变 ⇒ 进度兼容，**不升 CURRICULUM_VERSION**；只升 APP_BUILD 83→84。
- 新文本 21 个 ⇒ 补生成美英各 21 个音频（regen_audio_by_text.py，manifest 6064→6085），
  六线美/英缺失回到 0。旧括号文本的音频成孤儿（无害，勿 --prune 顺带清）。
- 备份：.workbuddy/baselines/*.before-parenfix。脚本：.workbuddy/tmp/fix_paren.py、scan_paren.py。
